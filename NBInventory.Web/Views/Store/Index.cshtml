@model IEnumerable<NBInventory.DTO.StoreDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Store_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Store_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Store_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Store_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Store_ID }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Store_ID })
        </td>
    </tr>
}

</table>
