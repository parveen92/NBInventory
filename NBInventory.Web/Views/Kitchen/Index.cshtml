@model IEnumerable<NBInventory.DTO.KitchenDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table"> 
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kitchen_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kitchen_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kitchen_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kitchen_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Kitchen_ID  }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Kitchen_ID })
        </td>
    </tr>
}

</table>
